<UserControl x:Class="YBehavior.Editor.FSMTransitionDataFrame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YBehavior.Editor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Renderer.Name}" Background="DarkGray"/>
        <DockPanel Name="ConditionPanel">
            <TextBlock Text="Condition" Background="LightBlue" Width="90"/>
            <TextBox Name="Condition" Text="{Binding Renderer.Event, UpdateSourceTrigger=PropertyChanged}"
                     local:InputBindingsManager.UpdatePropertySourceWhenEnterPressed="TextBox.Text"/>
        </DockPanel>
    </StackPanel>
</UserControl>
