<UserControl x:Class="YBehavior.Editor.UIVariable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YBehavior.Editor"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300" DataContextChanged="UserControl_DataContextChanged">
    <UserControl.Resources>
        <local:VariableValidColorConvertor x:Key="validConvertor"/>
        <BooleanToVisibilityConverter x:Key="visibilityConvertor"/>
        <local:VariableValueTypeConvertor x:Key="valueTypeConvertor"/>
        <local:VariableVariableTypeConvertor x:Key="variableTypeConvertor"/>
        <local:VariableTypeCountConvertor x:Key="typeCountConvertor"/>
        <local:InvertBoolConverter x:Key="invertBoolConvertor"/>
        <local:ValueConverterGroup x:Key="invertBoolAndVisibilityConvertor">
            <local:InvertBoolConverter/>
            <BooleanToVisibilityConverter/>
        </local:ValueConverterGroup>
    </UserControl.Resources>
    <DockPanel Background="LightBlue" Margin="2,2,2,2">
        <TextBlock Name="VKey" Text="{Binding Name}" FontSize="13" Width="90" DockPanel.Dock="Left" Background="LightCyan"/>
        <ComboBox Name="VTypes" Width="50" ItemsSource="{Binding vTypeSet}" SelectedItem="{Binding vType}"
            IsHitTestVisible="{Binding vTypeSet, Converter={StaticResource typeCountConvertor}}">
            <!--IsEditable="{Binding vTypeSet, Converter={StaticResource typeCountConvertor}}"
                  Focusable="False"
                  -->
            <ComboBox.Resources>
                <sys:Double x:Key="{x:Static SystemParameters.VerticalScrollBarWidthKey}">0</sys:Double>
            </ComboBox.Resources>
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Converter={StaticResource valueTypeConvertor}}" FontSize="12"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <CheckBox Width="20" Height="20" Name="Switcher" Background="Orange" Foreground="GreenYellow" DockPanel.Dock="Right" IsChecked="{Binding vbType, Converter={StaticResource variableTypeConvertor}}" Visibility="{Binding Path = CanSwitchConst, Converter={StaticResource visibilityConvertor}}"/>
        <Button Width="20" Height="20" Name="Remover" Background="Red" DockPanel.Dock="Right" Visibility="{Binding Path = CanBeRemoved,  Converter={StaticResource visibilityConvertor}, Mode=OneWay}" Click="Remover_Click"/>
        <ComboBox SelectedItem="{Binding Value}" ItemsSource="{Binding Candidates}" x:Name="VPointer" MinWidth="110" MaxDropDownHeight ="150"
                  Visibility="{Binding Path=IsChecked, ElementName=Switcher, Converter={StaticResource visibilityConvertor}}" 
                  Background="{Binding Path = IsValid, Converter={StaticResource validConvertor}}">
            <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ComboBox.ItemsPanel>
        </ComboBox>
        <Grid Visibility="{Binding Path=IsChecked, ElementName=Switcher, Converter={StaticResource invertBoolAndVisibilityConvertor}}">
            <TextBox Name="VConst" Text="{Binding Value}"  Background="{Binding Path = IsValid, Converter={StaticResource validConvertor}}"/>
            <ComboBox Name="VEnum" SelectedItem="{Binding Value}" ItemsSource="{Binding Enums}" Background="{Binding Path = IsValid, Converter={StaticResource validConvertor}}"/>
        </Grid>
    </DockPanel>
</UserControl>
